// Console (people + concepts) — feed slot wiring. Null slots render baseline.

import type { FeedSlots } from "../types"

export const consoleFeed: FeedSlots = {
  Card: null,
  FeedLoading: null,
  FeedEmpty: null,
  BottomNav: null,
  headerConfig: null,
  tabPagePaddingClass: null,
}
