// Stage (questions + stories) — feed slot wiring. Null slots render baseline.

import type { FeedSlots } from "../types"

export const stageFeed: FeedSlots = {
  Card: null,
  FeedLoading: null,
  FeedEmpty: null,
  BottomNav: null,
  headerConfig: null,
  tabPagePaddingClass: null,
}
